<h1>Posts from <%= @source.name %></h1>

<ul>
  <% @source.posts.reverse.each do |post| %>
    <li><%= link_to post.headline, source_post_path(@source,post) %> (<%= post.sub_count%> substitutions)</li>
  <% end %>  
</ul>
